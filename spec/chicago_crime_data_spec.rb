require 'spec_helper'

describe ChicagoCrimeData do
  it 'has a version number' do
    expect(ChicagoCrimeData::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
